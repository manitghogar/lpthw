#puts the prompt in the raw_input argument and assigns the answer to the value of variable.
age =  raw_input ("How old are you? ")
height = raw_input ("How tall are you? ")
weight = raw_input ("How much do you weigh? ")
#prints a string with format variables that call on the values of age, height, weight
print "So, you're %r old, %r tall and %r heavy." % (age, height, weight)


"""
Pydoc is a document generator and online help system. It automatically
generates documentation from Python modules. It tells you how functions work
and what to input in them.

Pydoc raw_input tells you what to put in the raw_input argument

Pydoc Open - allows you to open a file using the file() type, 
returns a file object.

Pydoc file - opens a file after specifying the mode (reading, writing or appending). 
The file will be created if it doesn't exist.

Pydoc os - provides help on module os - provides documentation on where python sits in 
the operating system

Pydoc sys - (not too sure about this one)
